{
  "name": "@mockapiserver/example",
  "version": "2.0.0",
  "description": "example setup",
  "author": "Richard Scoones <richard.scoones@gmail.com>",
  "main": "./server",
  "scripts": {
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --outfile=../../build/example/index.js",
    "watch": "esbuild index.ts --bundle --sourcemap --platform=node --watch  --outfile=../../build/example/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:rscoones/MockServer.git"
  },
  "devDependencies": {
    "typescript": "5.3.3"
  },
  "license": "MIT",
  "dependencies": {
    "@types/express": "^4.17.21",
    "@types/express-session": "^1.17.10",
    "express": "^4.18.2",
    "express-session": "^1.17.3"
  }
}
